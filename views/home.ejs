<%- include('partials/header') %>

<div class="container">
  <div class="home-hero">
    <img 
      src="/images/tasknest-logo.png" 
      width="150" 
      class="rounded-circle shadow-sm"
      alt="TaskNest Logo"
    >
    
    <h1>Welcome to TaskNest</h1>
    <p class="lead">Your University task management companion! :)</p>

    <div class="mt-4">
      <a href="/tasks" class="btn btn-primary btn-lg me-2">View Tasks</a>
      <% if (user) { %>
        <a href="/tasks/add" class="btn btn-success btn-lg">Add Task</a>
      <% } else { %>
        <a href="/register" class="btn btn-success btn-lg">Get Started</a>
      <% } %>
    </div>
  </div>
</div>

<%- include('partials/footer') %>